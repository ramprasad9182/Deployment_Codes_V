<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="logistic_screen.logisticDashboard" owl="1">
        <section id="logistics">
            <br/>
            <div class="dashboard-wrap">
                <div class="dashboard">
                    <!-- ===== Column 1 ===== -->
                    <div class="dashboard-column col-1" data-col="1">
                        <!-- Delivered -->
                        <div class="card">
                            <h3>Delivered</h3>
                            <div class="info-row" t-on-click="openDoneToday" style="cursor:pointer" >
                                <span>Today</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.done.today)"/>
                                </strong>
                            </div>
                            <div class="divider"/>
                            <div class="info-row" t-on-click="openDoneWeek" style="cursor:pointer" >
                                <span>Week</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.done.week)"/>
                                </strong>
                            </div>
                            <div class="divider"/>
                            <div class="info-row" t-on-click="openDoneMonth" style="cursor:pointer" >
                                <span>Month</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.done.month)"/>
                                </strong>
                            </div>
                            <div class="divider"/>
                        </div>
                        <div class="card">
                            <h3>Status of the LR’s</h3>
                            <!-- Delivered -->
                            <div class="status-row" t-on-click="openStatusDelivered" style="cursor:pointer" >
                                <span>Delivered</span>
                                <div class="status-bar">
                                    <div class="fill blue"
                                         t-att-style="'width:' + this.pct(state.status.delivered) + '%;'">
                                    </div>
                                </div>
                                <strong class="count">
                                    <t t-esc="state.loadingStatus ? '…' : fmt(state.status.delivered)"/>
                                </strong>
                            </div>
                            <!-- In Transit -->
                            <div class="status-row" t-on-click="openStatusInTransit" style="cursor:pointer" >
                                <span>In Transit</span>
                                <div class="status-bar">
                                    <div class="fill light-blue"
                                         t-att-style="'width:' + this.pct(state.status.in_transit) + '%;'">
                                    </div>

                                </div>
                                <strong class="count">
                                    <t t-esc="state.loadingStatus ? '…' : fmt(state.status.in_transit)"/>
                                </strong>
                            </div>
                            <!-- Delayed -->
                            <div class="status-row" t-on-click="openStatusDelayed" style="cursor:pointer" >
                                <span>Delayed</span>
                                <div class="status-bar">
                                    <div class="fill orange"
                                         t-att-style="'width:' + this.pct(state.status.delayed) + '%;'">
                                    </div>
                                </div>
                                <strong class="count">
                                    <t t-esc="state.loadingStatus ? '…' : fmt(state.status.delayed)"/>
                                </strong>
                            </div>
                        </div>
                        <!-- Delivered -->
                        <div class="card">
                            <h3>Upcoming Deliveries</h3>
                            <div class="info-row" t-on-click="openDraftToday" style="cursor:pointer" >
                                <span>Today</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.draft.today)"/>
                                </strong>
                            </div>
                            <div class="divider"/>

                            <div class="info-row" t-on-click="openDraftWeek" style="cursor:pointer" >
                                <span>Week</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.draft.week)"/>
                                </strong>
                            </div>
                            <div class="divider"/>
                            <div class="info-row" t-on-click="openDraftMonth" style="cursor:pointer" >
                                <span>Month</span>
                                <strong>
                                    <t t-esc="state.loadingDelivery ? '…' : fmt(state.draft.month)"/>
                                </strong>
                            </div>
                            <div class="divider"/>
                        </div>
                    </div>
                    <!-- ===== Column 2 ===== -->
                    <div class="dashboard-column col-2" data-col="2">
                        <!--Partial Delivered LRs-->
                        <div class="card">
                            <h3>Partial Delivered LRs</h3>
                            <div class="card2-content" t-on-click="openPartialDelivered" style="cursor:pointer" >
                                <strong class="big-number2" style="padding-left:30px">
                                    <t t-esc="state.loadingPartialDelivered ? '…' : fmt(state.partialDelivered)"/>
                                </strong>
                                <div class="bar-chart2">
                                    <div class="bar2" style="height: 20%"/>
                                    <div class="bar2" style="height: 40%"/>
                                    <div class="bar2" style="height: 60%"/>
                                    <div class="bar2" style="height: 90%"/>
                                    <div class="bar2" style="height: 70%"/>
                                    <div class="bar2" style="height: 50%"/>
                                </div>
                            </div>
                        </div>
                        <div class="card">
                            <div>
                                <div class="half half--left half--no-inner-divider">
                                    <div class="half-title">No. of Bales<br/>against LR
                                    </div>
                                    <div class="kv-table kv-table--tight">
                                        <div class="kv-row" t-on-click="() => this.openOpenParcels()" role="button" tabindex="0"
                                             style="cursor:pointer" t-att-title="'Open parcels (state=done)'">
                                            <div class="kv-key">Open</div>
                                            <div class="kv-value">
                                                <t t-esc="state.loadingOpenParcel ? '…' : this.fmt(state.openParcel.done)"/>
                                            </div>
                                        </div>
                                        <div class="kv-row" t-on-click="() => this.openUnopenParcels()" role="button" tabindex="0"
                                             style="cursor:pointer" t-att-title="'Unopened parcels (state=draft)'">
                                            <div class="kv-key">Unopen</div>
                                            <div class="kv-value">
                                                <t t-esc="state.loadingOpenParcel ? '…' : this.fmt(state.openParcel.draft)"/>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="card">
                            <div data-card="divisionwise-unopened">
                                <div class="card__header">
                                    <h3 class="card__title">Division Wise<br/>Unopened LR's
                                    </h3>
                                </div>
                                <div class="card__body">

                                    <t t-if="state.unopenedDivision.items.length">

                                        <t t-foreach="state.unopenedDivision.items"
                                           t-as="division"
                                           t-key="division.division_name">

                                            <div class="metric-row"
                                                 t-att-data-row="'division_' + division.division_name"
                                                 t-on-click="() => this.openDivisionDeliveries(division.division_name)"
                                                 role="button"
                                                 tabindex="0"
                                                 t-att-title="'Open deliveries for ' + division.division_name"
                                                 style="cursor: pointer;">

                                                <div class="metric-row__label">
                                                    <t t-esc="division.division_name"/>
                                                </div>

                                                <div class="metric-row__value">
                                                    <t t-esc="division.count"/>
                                                </div>

                                            </div>

                                            <div class="divider"/>

                                        </t>

                                    </t>

                                    <t t-else="">
                                        <div class="metric-row">
                                            <div class="metric-row__label">No Unopened Division</div>
                                            <div class="metric-row__value">0</div>
                                        </div>
                                        <div class="divider"/>
                                    </t>

                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- ===== Column 3 ===== -->
                    <div class="dashboard-column col-3" data-col="3">
                        <!--    Division Wise LR's      -->
                        <div class="card">
                            <h3>Top Delivered Products</h3>
                            <div class="tp-total">
                                <spa>Total:
                                    <t t-esc="state.loadingTopProducts ? '…' : this.fmt(state.topProducts.total)"/>
                                </spa>
                            </div>
                            <div class="tp-chart chartjs-render-monitor">
                                <t t-foreach="state.topProducts.items" t-as="it" t-key="it.key">
                                    <div class="tp-col" t-on-click="() => this.openTopProduct(it.key)"
                                         style="cursor: pointer;">
                                        <div class="tp-count">
                                            <t t-esc="this.fmt(it.count)"/>
                                        </div>

                                        <!-- Plan B: pixel height per bar -->
                                        <div class="tp-bar"
                                             t-att-title="it.product_name"
                                             t-att-style="'height:' + this.barPx(it.count, 140) + 'px;'">
                                        </div>

                                        <div class="tp-label" t-att-title="it.product_name">
                                            <t t-esc="it.parent_category"/>
                                        </div>
                                    </div>
                                </t>
                                <t t-if="!state.loadingTopProducts and ((state.topProducts.items || []).length === 0)">
                                    <div class="muted">No delivered records</div>
                                </t>
                            </div>
                        </div>
                        <!-- Card 3-4: Pending for GRC (Division-wise) -->
                        <div class="card card--s" data-card="grc-pending">
                            <div class="card__header">
                                <h3 class="card__title">
                                    Pending for GRC by
                                    <br/>
                                    Division-wise with Bale qty
                                </h3>
                            </div>
                            <div class="card__body">
                              <!-- Safety - check state and items existence -->
                              <t t-if="state.unopenedDivisionBales and state.unopenedDivisionBales.items and state.unopenedDivisionBales.items.length">
                                <t t-foreach="state.unopenedDivisionBales.items" t-as="division" t-key="division.division_name">
                                  <div class="metric-row"
                                       t-att-data-row="'division_' + division.division_name"
                                       t-on-click="() => this.openPendingGRCDivision(division.division_name)"
                                       role="button"
                                       tabindex="0"
                                       t-att-title="'Open deliveries for ' + division.division_name"
                                       style="cursor: pointer;">
                                    <div class="metric-row__label">
                                      <t t-esc="division.division_name"/>
                                    </div>
                                    <div class="metric-row__value">
                                      <t t-esc="division.parcel_bale_total"/>
                                    </div>
                                  </div>
                                  <div class="divider"/>
                                </t>
                              </t>
                              <t t-else="">
                                <div class="metric-row">
                                  <div class="metric-row__label">No Unopened Division</div>
                                  <div class="metric-row__value">0</div>
                                </div>
                              </t>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="dashboard-footer is-sticky">
                    <div class="footer-row" style="height:200px;">
                    </div>
                </div>
            </div>
        </section>
    </t>
</templates>