<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!--    #### Automatic Transportation tree View #####-->
    <record id="transport_check_tree" model="ir.ui.view">
        <field name="name">transport.check.tree</field>
        <field name="model">transport.check</field>

        <field name="arch" type="xml">
            <tree
                    string="Transport Check" create="0" multi_edit="1"
            >

                <field name="logistic_entry_number"/>
                <field name="logistic_lr_date"/>

                <field name="logistic_lr_number"/>
                <field name="logistic_date" widget="date" optional="show"/>
                <field name="transporter" widget="many2one_avatar_user"/>
                <field name="consignment_number"/>
                <field name="po_order_id" readonly="1"/>


                <field name="state"
                       decoration-success="state == 'sent'"
                       decoration-info="state == 'draft'"

                       widget="badge"
                       optional="hide"/>

                <field name="no_of_bales" optional="show"/>
                <field name="item_details" optional="show" column_invisible="1"/>

            </tree>
        </field>
    </record>

    <!--    #### Manual Transportation tree View #####-->
    <record id="manual_transport_check_tree" model="ir.ui.view">
        <field name="name">transport.check.tree</field>
        <field name="model">transport.check</field>

        <field name="arch" type="xml">
            <tree
                    string="Manual Transport Check" create="1" multi_edit="1"
            >

                <field name="logistic_entry_number"/>
                <field name="logistic_lr_date"/>

                <field name="logistic_lr_number"/>
                <field name="logistic_date" widget="date" optional="show"/>
                <field name="transporter" widget="many2one_avatar_user"/>
                <field name="consignment_number"/>
                <field name="po_order_id" readonly="1" column_invisible="1"/>


                <field name="state"
                       decoration-success="state == 'sent'"
                       decoration-info="state == 'draft'"

                       widget="badge"
                       optional="hide"/>

                <field name="no_of_bales" optional="show"/>
                <field name="item_details" optional="show" column_invisible="1"/>

            </tree>
        </field>
    </record>

    <!--    #### Transportation Check Search View #####-->
    <record id="transport_check_search_view" model="ir.ui.view">
        <field name="model">transport.check</field>
        <field name="name">transport.check.search</field>
        <field name="arch" type="xml">
            <search string="Transport check">
                <field name="transporter"/>
                <field name="po_order_id" invisible="1"/>
                <field name="logistic_entry_number"/>
                <field name="state"/>
                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" context="{'default_state': 'draft'}" />

            </search>
        </field>
    </record>


    <record id="transport_check_form" model="ir.ui.view">
        <field name="name">transport.check.form</field>
        <field name="model">transport.check</field>
        <field name="arch" type="xml">
            <form string="Transport Check" create="0">
                <header>

                    <button name="action_confirm" id="action_confirm" data-hotkey="q"
                            string="Confirm" class="btn-primary" type="object"
                            invisible="state == 'sent'"/>

                    <button name="reset_to_draft" id="reset_to_draft" data-hotkey="q"
                            string="Reset to Draft" class="btn-primary" type="object"
                            invisible="state != 'sent'"/>


                    <field name="state" widget="statusbar" statusbar_visible="draft,sent"/>
                </header>

                <sheet>
                    <group name="transport_header">
                        <group name="transport_details">
                            <field name="readonly_po_vendor" invisible="1"/>
                            <field name="logistic_entry_number"
                                   placeholder="Enter Logistic Entry Number .." readonly="logistic_entry_number" forcesave="1"/>

                            <field name="transporter"
                                   widget="res_partner_many2one"
                                   placeholder="Type to find a Transporter..." readonly="transporter" forcesave="1"
                                   options="{'no_create': True, 'no_open': True, 'no_edit':True}" domain="[('group_contact.name','=','Transporter')]"/>

                            <field name="logistic_lr_date"
                                   readonly="logistic_lr_date" forcesave="1"/>

                            <field name="logistic_lr_number"
                                   placeholder="Enter Logistic LR Number .." readonly="logistic_lr_number" forcesave="1"/>

                        </group>
                        <group name="transport_date_details">

                            <field name="logistic_date"
                                   readonly="logistic_date" forcesave="1"/>

                            <field name="consignment_number"
                                   placeholder="Enter Consignment Number .."
                                   readonly="consignment_number"/>

                            <field name="po_order_id" readonly="readonly_po_vendor" options="{'no_create': True, 'no_open': True, 'no_edit':True}" invisible="not po_order_id"/>

                            <field name="no_of_bales"
                                   placeholder="Enter Number Of Bales.."
                                   readonly="no_of_bales" forcesave="1"/>
                            <field name="product_id"
                                   readonly="product_id" domain="[('detailed_type', '=', 'consu')]" options="{'no_create': True, 'no_open': True, 'no_edit':True}"/>

                            <field name="city"
                                   placeholder="Enter City.."
                                   readonly="city" forcesave="1"/>

                            <field name="item_details" invisible="not po_order_id"/>

                            <field name="invoice_list"
                                   placeholder="Enter Invoice list"
                                   readonly="state in ['sent']"/>

                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>


    <record id="action_to_transport_check" model="ir.actions.act_window">
        <field name="name">Transport Check</field>
        <field name="res_model">transport.check</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="transport_check_search_view"/>
        <field name="domain">[('transport_entry_types', '=', 'automatic')]</field>
        <field name="context">{"search_default_state":'draft' 'create': False}</field>
         <field name="views" eval="[(ref('transport_check_tree'), 'tree'), (ref('transport_check_form'), 'form')]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a Transport Check
            </p>
        </field>
    </record>



     <record id="manual_action_to_transport_check" model="ir.actions.act_window">
        <field name="name">Manual Transport Check</field>
        <field name="res_model">transport.check</field>
        <field name="view_mode">tree,form</field>
         <field name="search_view_id" ref="transport_check_search_view"/>
         <field name="view_id" ref="manual_transport_check_tree"/>

         <field name="domain">[('transport_entry_types', '=', 'manual')]</field>
         <field name="context">{"search_default_state":'draft'}</field>
<!--        <field name="search_view_id" ref="transport_check_search_view"/>-->
<!--        <field name="context">{"search_default_state":'draft'}</field>-->

    </record>

<!--     <menuitem id="transport_menu_root"-->
<!--              name="Transport Check"-->
<!--              action="action_to_transport_check"-->
<!--              parent="logistic_menu_entry"-->
<!--              />-->

    <menuitem id="manual_transport_menu_root"
              name="Transport Check"
              action="manual_action_to_transport_check"
              parent="logistic_menu_entry"
              />
</odoo>